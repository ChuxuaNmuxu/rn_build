import React from 'react';
import { View, Text } from 'react-native';

class QuestionOrderModal extends React.PureComponent {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <View>
        <Text>模块</Text>
      </View>
    );
  }
}

export default QuestionOrderModal;
